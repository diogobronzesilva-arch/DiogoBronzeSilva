# Canonical host and custom error page.
ErrorDocument 404 /404.html

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{HTTP_HOST} ^www\.diogobronzesilva\.com$ [NC]
  RewriteRule ^ https://diogobronzesilva.com%{REQUEST_URI} [R=301,L,NE]
</IfModule>

# Security headers for the static site. CSP is managed by the Hostinger CDN.
<IfModule mod_headers.c>
  Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
  Header always set X-Content-Type-Options "nosniff"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"
  Header always set X-Frame-Options "SAMEORIGIN"
</IfModule>
